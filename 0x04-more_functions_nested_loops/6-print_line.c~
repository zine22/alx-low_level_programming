#include <main.h>

/**
* print_line - to print lines
*
* Return: always 0
*
* @n: random number
*/

void print_line(int n)
{
int n;
int m;
int k;

while (m = 95)
{
if (n > 0)
{
k =  n * m
_putchar(k);
}
else
_putchar('\n');
}
return (0);
}
