#include "main.h"

/**
* _abs - computes the absolute value of an int
*
* Return: Always 0 (success)
*
* @n: the integer to be checked
*/

int _abs(int n)
{
for (n < 0)
{
_putchar(45n);
}
else
{
_putchar(n);
}
return (0);
}
