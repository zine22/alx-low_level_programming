#include "main.h"

/**
* main - entry point
*
* @num1: number
* @num2: number
* @mul: multiply two numbers
*
* Return: always 0
*/

int main(void)
{
float num1, num2;
int mul, i, j;

for (i = 0; i < num1; i++)
;
for (j = 0; j < num2; j++)
;
_putchar(num1 * num2);

return (0);

}
