#include <stdio.h>

/**
* main - print program name using argv and argc func
*
* @argv: list
* @argc: int
*
* Return: 0
*/

int main(int argc, char *argv[])
{
int i;
if (i == 0)
{
printf("argv[%d] = %s\n", i, argv[i]);
}
return (0);
}
