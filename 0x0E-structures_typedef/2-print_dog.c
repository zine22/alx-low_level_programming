#include "dog.h"
#include <stdio.h>

/**
* print_dog - print a struct dog
*
* @d: member
*
* Return: void
*/

void print_dog(struct dog *d)
{
if (d)
{
if (name == NULL)
printf("%s", name = (nil));
else
printf("%s\n", name);
if (owner == NULL)
printf("%s", owner = (nil));
else
printf("%s\n", name);

printf("%f\n", age);
}

}
